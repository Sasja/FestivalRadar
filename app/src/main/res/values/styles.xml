<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
    </style>

    <style name="MainActionTheme" parent="AppTheme">
        <item name="android:actionBarStyle">@style/MainActionBar</item>
        <!--Support lib compatibility-->
        <item name="actionBarStyle">@style/MainActionBar</item>
    </style>

    <style name="MainActionBar" parent="Widget.AppCompat.Light.ActionBar.Solid">
        <!--will remove the 'clique' title from action bar-->
        <item name="android:displayOptions"></item>
        <!--Support lib compatibility-->
        <item name="displayOptions"></item>
    </style>

</resources>